import re\n\nclass JobDescriptionParser:\n    def __init__(self, job_description):\n        self.job_description = job_description\n\n    def extract_skills(self):\n        # A simple regex to find skills in parentheses or quoted\n        skills_pattern = r'\b(?:[A-Z][a-z]*\s?)+\b'\n        return re.findall(skills_pattern, self.job_description)\n\n    def get_job_title(self):\n        title_pattern = r'(?<=Job Title: )(.*?)(?=\n)'\n        return re.search(title_pattern, self.job_description).group(0)\n\n    def get_company_name(self):\n        company_pattern = r'(?<=Company: )(.*?)(?=\n)'\n        return re.search(company_pattern, self.job_description).group(0)\n\n# Example usage\n# job_desc = '''\n# Job Title: Software Engineer\n# Company: Tech Corp\n# Skills: Python, Java, SQL\n# '''\n# parser = JobDescriptionParser(job_desc)\n# print(parser.extract_skills())  # Output: ['Python', 'Java', 'SQL']\n